// ✅ 10টি Ternary Operator Practice Problems

let num, result;
num = 10;

// 1. Even or Odd
result = num % 2 === 0 ? "Even" : "Odd";

console.log(result);

// score check
let score = 85;

let grade = 85 >= 90 ? "A+" : 85 >= 80 ? "B" : 85 >= 70 ? "C" : "F";

console.log(grade);
